# This file is just for "moved" declarations for backward compatibility
#
# Nothing here yet: this module has not renamed any of its own resources.
#
# Note that a `moved` block cannot help anyone migrating from `aws-ia/ipam`. A
# `moved` block can only name addresses relative to its own module instance, and
# a migrating consumer's old state lives under a different module call in their
# root configuration. Those blocks have to be written by the consumer, in their
# root — see `docs/migration-aws-ia-v1.md`, which spells out the exact ones.
